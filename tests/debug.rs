/*
mod test_debug {
    use libaki_sort::*;
    #[test]
    fn size_of() {
        assert_eq!(std::mem::size_of::<libaki_sort::sort_key::KeyColumns>(), 16);
        assert_eq!(std::mem::size_of::<libaki_sort::sort_key::string::SortLinesBufferString>(), 16);
        assert_eq!(std::mem::size_of::<libaki_sort::sort_key::string::SortLine>(), 16);
    }
}
*/
